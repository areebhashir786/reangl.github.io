export interface DataInterface {
  name: string;
  days: string;
  price: string;
  text: string;
  description: string;
  bids: string;
  tag1?: string;
  tag2?: string;
  tag3?: string;
  tag4?: string;
  //we use ? for optional fields
}
